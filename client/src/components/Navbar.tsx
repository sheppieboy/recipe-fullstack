
import { Typography } from "@mui/material"

import { Box } from "@mui/system"

export const Navbar = () =>{
    return (<Box>
        <Typography >
            Navbar
        </Typography>
    </Box>)
}